import { SearchbarSkeleton } from "./components/search-bar";

export default function () {
  return (
    <main className="flex items-center justify-center">
      <SearchbarSkeleton />
    </main>
  );
}
